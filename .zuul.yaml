- project:
    check:
      jobs:
        - pipenv:
            vars:
              pipenv_extra_args:
                - "python3 setup.py test"
        - linters:
            vars:
              linters:
                - coala
    gate:
      queue: core
      jobs:
        - linters:
            vars:
              linters:
                - coala

    release:
      jobs:
        - upload-pypi-sesheta
